/* =====================================================
   SOGRUB Bitácora — Storage (delegado a Firebase)
   Todas las funciones CRUD están en js/firebase.js.
   Este archivo se mantiene por compatibilidad de orden
   de carga en el HTML.
   ===================================================== */
'use strict';
// getCollection, saveCollection, generateId, addItem,
// updateItem, deleteItem, getItem, getConfig, saveConfig,
// updateConfig, KEYS e initializeData
// → todos definidos en js/firebase.js (cargado antes)
